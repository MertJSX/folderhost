export interface Shared {
    id: string,
    username: string,
    userID: number,
    displayName: string,
    path: string,
    password: string,
    expires_at: string,
    downloadCount: number,
    fileExtension: string,
    created_at: string
}